export default function Home() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-4xl font-bold tracking-tight">KillScore</h1>
      <p className="text-lg text-zinc-500 max-w-xl text-center">
        100% coverage. 31% mutation score. The SaaS release gate that scores
        whether your AI-written tests can actually catch a bug.
      </p>
    </main>
  );
}
